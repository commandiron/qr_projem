import 'package:flutter/material.dart';

class SignInPage extends StatelessWidget {
  const SignInPage({Key? key}) : super(key: key);

  static const pageIndex = 3;

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("SingInPage"),
    );
  }
}
