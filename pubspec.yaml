name: qr_projem
description: Qr Projem

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.19.1 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  url_strategy: ^0.2.0
  google_fonts: ^4.0.1
  flutter_bloc: ^8.1.2
  http: ^0.13.5
  firebase_core: ^2.6.1
  firebase_auth: ^4.2.8
  firebase_database: ^10.0.13
  mask_text_input_formatter: ^2.4.0
  firebase_auth_platform_interface: ^6.11.10
  flutter_verification_code: ^1.1.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/images/
    - assets/images/logo/
